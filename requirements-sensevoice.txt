# SenseVoice-Small ONNX path for transcribe_sensevoice.py (Intel Mac friendly).
# Install on top of the main cantonese-asr venv:
#   pip install -r requirements-sensevoice.txt
#
# Uses funasr-onnx + pre-quantized weights (~230 MB, auto-downloaded on first run).
# Skips full funasr / llvmlite which often fail to build on macOS x86.
funasr-onnx>=0.4.1
onnxruntime>=1.16.0
onnx>=1.14.0
scipy>=1.10.0
PyYAML>=6.0
sentencepiece>=0.2.0
kaldi-native-fbank>=1.18.0
huggingface_hub>=0.23.0
librosa>=0.10.0
lazy_loader>=0.4
decorator>=5.0
audioread>=3.0
joblib>=1.3
pooch>=1.6
soxr>=0.3
msgpack>=1.0
